import { StyleSheet } from 'react-native';

const styles = {
  modal: {
    flex: 1,
    flexDirection: 'column',
    marginTop: 22,
    alignItems: 'center',
    padding: 20,
  },

  filler: {
    flex: 1,
  },

  button: {
  }

}

export default StyleSheet.create(styles);
