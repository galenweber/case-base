import { StyleSheet } from 'react-native';

const styles = {

  numerator: {
    fontSize: 10,
    paddingBottom: 10,
  },

  denominator: {
    fontSize: 10,
    paddingTop: 5,
  },

  fraction: {
    flexDirection: 'row',
    height: 16,
  },

};

export default StyleSheet.create(styles);
