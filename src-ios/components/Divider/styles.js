import { StyleSheet } from 'react-native';

const styles = {

  divider: {
    borderWidth: 0.5,
    borderColor: '#d6d7da',
    marginTop: 5,
    marginBottom: 20,
  },

};

export default StyleSheet.create(styles);
