import { StyleSheet } from 'react-native';

const styles = {

  textInput: {
    height: 40,
    textAlign: 'right',
    paddingRight: 10,
    borderWidth: 0,
  },

};

export default StyleSheet.create(styles);

