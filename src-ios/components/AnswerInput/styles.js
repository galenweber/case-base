import { StyleSheet } from 'react-native';

const styles = {

  row: {
    flexDirection: 'row',
  },

  input: {
    borderWidth: 0.5,
    borderColor: '#d6d7da',
    flex: 1,
    height: 40,
  },
};

export default StyleSheet.create(styles);


