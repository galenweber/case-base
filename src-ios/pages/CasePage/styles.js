import { StyleSheet } from 'react-native';

const styles = {
  container: {
    padding: 20,
    paddingBottom: 100,
    backgroundColor: '#FFFFFF',
  },
}

export default StyleSheet.create(styles);


