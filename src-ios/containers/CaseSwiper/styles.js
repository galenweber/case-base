import { StyleSheet } from 'react-native';

const styles = {
  outer: {
    flex: 1,
    backgroundColor: 'white',
  },

  container: {
    flex: 1,
  },

  wrapper: {
  },
};

export default StyleSheet.create(styles);

